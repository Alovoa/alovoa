# Edge Deployment

Use the Cloudflare Worker module in:

- `deploy/edge/platform-worker`

This provides a lightweight shared platform API backed by Neon Postgres.

- Worker routes and deployment steps: `deploy/edge/platform-worker/README.md`
- Shared schema: `deploy/shared/sql/001_platform_core.sql`
